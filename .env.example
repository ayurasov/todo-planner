# mock -- localStorage-реализации репозиториев (текущий режим по умолчанию).
# http -- реальный backend (Flask) через src/repositories/http/apiClient.js.
VITE_API_MODE=mock

# Базовый URL backend API (используется только при VITE_API_MODE=http).
VITE_API_BASE_URL=/api
